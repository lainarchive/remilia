# Fastfetch

This directory contains the Remilia Fastfetch config and its local logo asset.

Expected files:
- `config.jsonc`
- `manga.six`

The import helper at `scripts/import-terminal-config.ps1` copies the existing working Fastfetch files into this directory without changing their contents.
